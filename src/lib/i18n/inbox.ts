/**
 * Inbox + thread shared Mini App copy. Populated as components migrate
 * (see plan Task 4). Sub-groups: `row`, `thread`, `message`, `mediaPicker`,
 * `assignTeacher`, `feedback`, `dateSeparator`.
 */

export const inbox = {} as const;
