/**
 * Student-side Mini App copy. Populated as components migrate (see plan
 * Task 3). Sub-groups: `menu`, `summary`, `freeze`, `referrals`,
 * `responseWindow`, `home`.
 */

export const student = {} as const;
